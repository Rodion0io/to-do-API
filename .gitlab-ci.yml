stages:
  - build
  - linter
  - test
  - deploy-staging
  - deploy-production

#Шаблоны для ветки dev
.build-template-dev:
  script:
    - docker build -t app:dev .

.linter-template-dev:
  script:
    #    Использую один и тот же образ приложения в обеих ветках, но по-хорошему нужно разные образы
    - docker run --rm -d app:latest dotnet format --check
  needs:
    - build-job-dev

.test-template-dev:
  script:
    - docker run --rm -d app:dev dotnet test
  artifacts:
    paths:
      - ./Dockerfile
    when: on_success
    expire_in: 30 days
  needs:
    - linter-job-dev

.deploy-template-dev:
  script:
    - docker-compose up -d
  environment:
    name: staging
  needs:
    - test-job-dev

#Шаблоны для ветки main
.build-template-main:
  script:
    - docker build -t app:latest .

.linter-template-main:
  script:
    - docker run --rm -d app:latest dotnet format --check
  needs:
    - build-job-main

.test-template-main:
  script:
    - docker run --rm -d app:latest dotnet test
  artifacts:
    paths:
      - ./Dockerfile
    when: on_success
    expire_in: 30 days
  needs:
    - linter-job-main

.deploy-template-main:
  script:
    - docker-compose up -d
  environment:
    name: production
  needs:
    - test-job-main

#ветка dev
build-job-dev:
  stage: build
  extends: .build-template-dev
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev"'

linter-job-dev:
  stage: linter
  extends: .linter-template-dev
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev"'

test-job-dev:
  stage: test
  extends: .test-template-dev
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev"'

deploy-staging:
  stage: deploy-staging
  extends: .deploy-template-dev
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev"'

#ветка main
build-job-main:
  stage: build
  extends: .build-template-main
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'

linter-job-main:
  stage: linter
  extends: .linter-template-main
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'

test-job-main:
  stage: test
  extends: .test-template-main
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'

deploy-production:
  stage: deploy-production
  extends: .deploy-template-main
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
